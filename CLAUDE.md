# CLAUDE.md

Инструкции для Claude Code в этом репозитории.

## Что это за проект

Набор AutoLISP-модулей (`LEP-*.LSP`) для автоматизации черчения ЛЭП
(линий электропередач) в AutoCAD 2025 — трассировка маршрута,
расстановка опор, нарезка пролётов, выгрузка ведомости.

Пользователь — не программист и сам почти не пишет AutoLISP. Поэтому
комментарии в коде подробные намеренно: они объясняют не только ЧТО
делает код, но и ПОЧЕМУ именно так. Это плата, а не раздутость —
сокращать их не надо.

## Проект опубликован на GitHub

Репозиторий: **https://github.com/vdovikovva/lep-autocad-powerline** (публичный).

Локальная папка и GitHub — это одно и то же, а не копия «на всякий случай».
После каждой значимой правки коммить и пушить: `git push origin main`.
Если этого не сделать, снаружи люди видят старую версию кода, а README врёт.

Что обновлять вместе с кодом:

| Изменилось | Что править |
|---|---|
| появилась/переименована команда `C:LEP-*` | таблицу команд в `README.md` **и** `README.ru.md` |
| изменился порядок работы | схему `LEP-SCALE → LEP-ROUTE → ...` в обоих README |
| изменилась установка | раздел «Установка» в обоих README |
| поймана и починена бага | `LEP-README.md`, как и раньше |

**Документация двуязычная.** `README.md` — английский, `README.ru.md` —
русский. Правишь один — правь и второй, иначе они разъезжаются. Подробная
документация (`LEP-README.md`) остаётся только на русском, это внутренний
журнал, переводить его не нужно.

## Лицензия: source-available, а не open source

`LICENSE` / `LICENSE.ru` — собственная лицензия автора. Код открыт для
чтения, но любое использование — запуск, изменение, выпуск чертежей —
требует письменного разрешения. Это **не** свободное ПО.

Отсюда следует:

- не предлагать и не подставлять сюда MIT, Apache, GPL и прочие открытые
  лицензии;
- не описывать проект в README, в статьях и в описании репозитория как
  «open source» — правильная формулировка «source-available»;
- вопросы вида «а можно я возьму код» решает автор, а не лицензия.

Репозиторий один раз уже был опубликован под MIT — лицензию заменили и
переписали историю коммитов. Повторять эту ошибку не надо.

## Хук, который не пускает лишнее в коммит

`.githooks/pre-commit` смотрит на то, что попало в индекс, и **блокирует**
коммит, если там оказались ключи и токены, дампы, архивы, `.DS_Store` или
файл крупнее 5 МБ. Для этого проекта это в первую очередь про чертежи:
`*.dwg` уже закрыты в `.gitignore`, но чертежи заказчиков не должны попадать
в публичный репозиторий и через `git add -f`.

Хук лежит в репозитории, но git не включает его сам — в свежем клоне надо
один раз выполнить:

```
git config core.hooksPath .githooks
```

Проверить: `git config core.hooksPath` должен вернуть `.githooks`.
Если тревога ложная, разовый обход — `git commit --no-verify`; но правильнее
дописать исключение в переменную `ALLOW` внутри хука, тогда оно останется.

## Где что искать

Один факт должен жить в ОДНОМ месте. Раскладка такая:

| Где | Что там |
|---|---|
| **шапка функции в `.LSP`** | как работает конкретный код и почему так |
| **[LEP-README.md](LEP-README.md)** | как работает программа целиком, история правок, что уже готово, инструкция пользователю |
| **тела коммитов** | зачем сделана правка и как её откатить |
| **этот файл** | правила, ловушки и живые ограничения |

Не пересказывай сюда то, что уже есть в трёх местах выше. Аудит
2026-08-05 показал: на 3864 строки кода приходилось 11 264 строки
прозы в четырёх местах, и `CLAUDE.md` успел стать вторым журналом
изменений (505 строк историй багов с датами).

## Что попадает в этот файл

Факт попадает сюда, только если верны ВСЕ ТРИ условия:

1. этого **нельзя узнать из кода** (правило, договорённость, внешнее
   требование, результат живого теста);
2. это нужно знать **до** того, как откроешь файл;
3. если забыть — **сломается молча**, без ошибки на экране.

История правки («было так, поймали тогда-то, стало так») сюда не
попадает **никогда** — она уже в README и в коммите. Достаточно самого
инварианта.

Блок «⚠️ НЕ ПРОВЕРЕНО ВЖИВУЮ» живёт здесь до подтверждения проверки
пользователем, потом уезжает в README одной строкой.

## Загрузка и файлы

`LEP-START.LSP` грузит все модули через Support File Search Path /
`findfile` и сам регистрирует СВОЮ папку в `TRUSTEDPATHS` (через
`(findfile "LEP-START.LSP")`, не через папку DWG). Папка с модулями
добавлена в Support Path — **разовая настройка НА КОМПЬЮТЕР**, после
неё DWG может лежать где угодно. У нового пользователя без этой
настройки `LEP-START.LSP` явно предупредит при загрузке.

Загружается автоматически при открытии любого чертежа через
`acaddoc.lsp`. ⚠️ `acaddoc.lsp` — зарезервированное имя, AutoCAD грузит
ТОЛЬКО ПЕРВЫЙ найденный во всём Support Path: если появится другой
плагин с таким же файлом, придётся объединять вызовы в одном файле.
Ручной `APPLOAD`/`(load "LEP-START.LSP")` тоже работает — удобно для
перезагрузки кода после правок без переоткрытия чертежа.

`(C:LEP-SETUP)` вызывается автоматически после загрузки модулей —
слои создаются без отдельной команды.

Не хардкодь пути пользователя. В git идут только исходники
(`.LSP`/`.DCL`/`.md`) — DWG, lock-файлы, бэкапы и выгруженные
ведомости в `.gitignore`.

## Структура модулей

- **`LEP-CONFIG.LSP`** — все настраиваемые параметры: блок опоры,
  геометрия символа, типы проводов/кабелей по классу напряжения,
  проценты и метры запаса, допуски, порог округления.
- **`LEP-START.LSP`** — загрузка модулей, `TRUSTEDPATHS`, автозапуск
  `LEP-SETUP` (см. раздел выше).
- **`LEP-SETUP.LSP`** — создаёт слои проекта, текстовый стиль
  (`LEP:CreateTextStyle`) и стиль выносок (`LEP:CreateMLeaderStyle`).
  ⚠️ `MLEADERSTYLE` — не классическая таблица AutoCAD, `tblobjname`
  для него возвращает `nil`; стиль "Standard" правится напрямую через
  `dictsearch`/`entget`/`subst`/`entmod`.
  ⚠️ `entmake` для таблицы `STYLE` требует маркеры подкласса
  (`AcDbSymbolTableRecord`/`AcDbTextStyleTableRecord`) — без них запись
  создаётся, но `setvar "TEXTSTYLE"` её отклоняет.
  ⚠️ `"ISOCPEUR"` (БЕЗ `.shx`) — TrueType-шрифт, не SHX. `isocp.shx` НЕ
  содержит кириллицу, SHX-шрифты для русских подписей не предлагать.
  ⚠️ Угол наклона у `STYLE` (группа DXF 50) — в РАДИАНАХ, хотя
  `*LEP-TextStyle-ObliqueAngle*` хранится в градусах (перевод — в
  `LEP:CreateTextStyle`). У большинства остальных `entmake`-углов в
  проекте — радианы, но это не общее правило, проверяй каждый раз.
- **`LEP-SCALE.LSP`** — калибровка масштаба подложки по двум точкам.
- **`LEP-ROUTE.LSP`** — черчение маршрута (полилиния), присвоение ID,
  подпись "Маршрут NN", вопрос о классе напряжения (ОДИН РАЗ на весь
  маршрут, хранится в XDATA `VCLASS`). Служебный слой
  `*LEP-Layer-Route*` — непечатаемый; `LEP-ROUTEVIS` переключает его
  видимость.
- **`LEP-POLESYM.LSP`** — символ опоры (трапеция + крест). Создаётся
  через `entmake` из `LEP-START.LSP`. Число трапеций — по
  `*LEP-PoleStands*`.
  ⚠️ **Блок создаётся ТОЛЬКО если его ещё нет** — в отличие от слоёв,
  НЕ передефайнится при перезагрузке. Меняешь геометрию — блок надо
  удалить из чертежа вручную, иначе правка не проявится.
  ⚠️ Угол разворота откоса у 2-/3-стоечных опор — **черновая прикидка**
  (перпендикулярно маршруту), точный угол ждём от коллеги. Придёт —
  править только `LEP:InsertPoleGroup`.
- **`LEP-POLE.LSP` / `.DCL`** — ручная расстановка опор в цикле:
  проекция на трассу, вершина, `GEOMCONSTRAINT`, символ, подпись.
  `LEP-POLE-ALONE` — опора без привязки к трассе.
  `LEP-POLE-DELETE`/`LEP-POLE-MOVE` — опора это блок + отдельная
  подпись + откосы без связи между ними, поэтому удаление идёт через
  `LEP:DeletePoleGroup` (подпись по `LABELHANDLE`, откосы по признаку
  «тот же блок рядом, но БЕЗ XDATA»).
  ⚠️ `LEP-POLE-ALONE` привязывается к БЛИЖАЙШЕЙ трассе геометрически —
  **известный риск неверной привязки на границе территорий двух
  заявителей**, см. README.
  ⚠️ `LEP-POLE-MOVE` работает только с `TYPE = "POLE"`; КТП/киоск им не
  переносятся. Текст подписи переносится ЦЕЛИКОМ, а не собирается
  заново — дописанные строки («ПУ», «+ СТП-XX кВА») нигде в XDATA не
  хранятся, восстановить их по кускам нечем.
  ⚠️ После каждого `GEOMCONSTRAINT` нужен пустой `(command)` (Escape) —
  иначе неудачный констрейнт подвешивает команду.
- **`LEP-END.LSP`** — оконечное оборудование: ПУ, МТП, СТП, КТП,
  кабельный киоск, точка разрыва участка КЛ. ОДНА концовка за запуск,
  без цикла.
  ⚠️ **Угол маршрута и угол разворота блока (`LEP:GetPoleRotation`) —
  РАЗНЫЕ углы, отличаются на 90°.** Их уже путали (опоры МТП уезжали
  перпендикулярно трассе). Не путать снова.
  ⚠️ **Заводя новый тип опоры — внеси его в ОБА списка**:
  `*LEP-PoleStands*` (марка стойки + число трапеций) и
  `*LEP-PoleTypes*` (иначе тип не выбрать при обычной расстановке).
  ⚠️ Тип опоры считается ОДИН РАЗ в переменную и дальше используется
  везде (вставка, подпись, XDATA) — подставь его в трёх местах
  отдельно, и забытое место даст «на чертеже одно, в подписи другое,
  в CSV третье», а глазами это не видно.
- **`LEP-SPAN.LSP`** — классификация пролётов между опорами, выбор
  ВЛ/КЛ и марки, способ прокладки КЛ (ГНБ/траншея), длины, подписи,
  размерные линии. `LEP-SPAN-REPAIR` чинит осиротевшие подписи.
  ⚠️ **НЕ ПУТАТЬ ДВЕ ФУНКЦИИ ДЛИНЫ**: `LEP:GetRealLength` → XDATA
  `LENGTH` → **ведомость** (все запасы + число фаз);
  `LEP:GetDisplayLength` → **подпись на чертеже** (только физика).
  Это осознанное разделение, а не дублирование.
  ⚠️ Запас пишется в XDATA пролёта (`RESERVE`) при создании — и его
  ОБЯЗАТЕЛЬНО переносить в `LEP:SetSpanLabelHandle` /
  `LEP:ReassignSpanRoute`, иначе `LEP:SetData` сотрёт его целиком.
  ⚠️ Множитель числа фаз (×3 на 10/6 кВ) применяется к ПРОВОДУ и НЕ
  применяется к КЛ даже на 10 кВ — кабель трёхжильный, это одна
  строительная длина.
  ⚠️ Округление длины — ОДИН раз, в самом конце, ПОСЛЕ умножения на
  фазы. Иначе хвостик округления докидывается трижды.
  ⚠️ Через маркер `TRANS` **не объединяется ГНБ**, всё остальное
  объединяется по обычным правилам (прокол дискретен — вышел бур,
  кончился прокол; траншея непрерывна). Проверка стоит в самом начале
  `LEP:GetSpanNeighbors`, ДО сравнения полей: сравнением XDATA этот
  случай не ловится в принципе — у двух проколов совпадают марка,
  статус И способ.
  ⚠️ `METHOD` входит в условие объединения подписей наравне с маркой и
  статусом — иначе «прокол, потом траншея» слиплись бы в одну подпись.
  ⚠️ `handent` с `nil` ПАДАЕТ — хендл конца проверять перед вызовом.
  ⚠️ Пролёт НЕ связан через `GEOMCONSTRAINT` с опорами: при переносе
  опоры пролёт придётся перечертить.
  ⚠️ `LEP:GetNearestVertexIndex` возвращает индекс ВСЕГДА, без всякой
  проверки расстояния — проверять обязана вызывающая сторона (для
  этого есть `LEP:EnsureVertexAtPoint`).
  ⚠️ **Геометрия спрашивается РАНЬШЕ XDATA.** Сначала ищем маршрут,
  физически проходящий через обе кликнутые точки
  (`LEP:FindRoutesThroughPoints`), и только если такого нет —
  спрашиваем про Т-образное подключение. Обратный порядок делал
  недоступным целый нормальный сценарий (маршрут 2 вливается в
  маршрут 1 и идёт с ним до общей ТП): «Да» рисовало прямую линию
  через всё поле, «Нет» отказывало. XDATA помнит, кем опору СОЗДАЛИ,
  геометрия — как трасса лежит СЕЙЧАС; для влившегося маршрута верна
  вторая.
  ⚠️ Условие работы этого механизма: **полилиния второго маршрута
  должна быть доведена до конечного объекта**. Нарисована до места
  слияния — геометрии нечего находить.
  ⚠️ **Режим по умолчанию — ОДИН пролёт на весь маршрут**, от первой
  опоры до конечного объекта (решение пользователя 2026-08-06). Дробить
  маршрут на пролёты по парам опор незачем: размеры программа ставит
  сама на каждый участок. **Исключение — ГНБ**: запасные трубы
  считаются по числу пролётов, целиком выйдет одна труба вместо
  нескольких, и на чертеже никаких признаков ошибки.
  ⚠️ **Параллельный сдвиг считается ПОСЕГМЕНТНО и ГЕОМЕТРИЧЕСКИ**
  (`LEP:CountOverlappingSpans`), а не по совпадению пары хендлов опор.
  Проверка по хендлам частичное перекрытие не видит в принципе — у
  влившегося маршрута пары концов разные, и провода ложились друг на
  друга. Каждый чужой пролёт считается ОДИН раз, даже если наш отрезок
  накрывает несколько его отрезков.
  ⚠️ **Сдвиг ≠ геометрия.** Длина пролёта и размерные линии считаются
  по НЕсдвинутым вершинам (`SubVertsRaw`); сдвинутая копия — только для
  рисования провода. Раньше это держалось само собой, теперь надо
  соблюдать явно.
  ⚠️ Шаг сдвига 0,3 м, трапеция опоры ~±0,5 м поперёк трассы →
  **три провода помещаются в символ опоры, четвёртый вылезает**. Так
  решено сознательно.
  ⚠️ **Дубли размерных линий проверяются ПО МЕСТУ, а не по маршруту**
  (`LEP:DimensionAlreadyThere`): середина нового участка лежит на
  существующем размере И они параллельны → не рисуем. Чертёж про
  маршруты ничего не знает, размер — просто объект на слое `LEP-DIM`.
  Проверка именно по СЕРЕДИНЕ, а не по концам: у второго маршрута
  вершины стоят не на опорах, его участок длиннее и накрывает
  несколько старых размеров, концы не совпадают ни с чем. Геометрия
  размера — DXF-группы 13/14 у `DIMENSION`, подтверждено живым тестом.
- **`LEP-REPORT.LSP`** — выгрузка ведомости в CSV. Без ActiveX, данные
  из XDATA. Считает только Проект., блоками по трассам + «ИТОГО».
  ⚠️ **CSV пишется в UTF-8 с BOM**: `(open path "w" "utf8-bom")` —
  третий аргумент `open`, AutoCAD 2021+. Без него `open` пишет
  Windows-1251; просто `"utf8"` без BOM Excel не распознаёт. Если
  где-то ещё понадобится файл с кириллицей — сразу `"utf8-bom"`.
  ⚠️ В ведомость идут **стойки, а не типы опор** (закупают
  железобетонные изделия). Перевод — `*LEP-PoleStands*`. Тип не найден
  в справочнике → ГРОМКАЯ строка в CSV + предупреждение в консоль,
  опора считается 1 шт.: молча потерянный материал — худшее, что эта
  программа может сделать.
  ⚠️ **Один прокол = один пролёт ГНБ** (ЭТАП 1.7). Никакой
  группировки. Цена принята сознательно: сплошной прокол мимо
  промежуточной опоры даст лишнюю запасную трубу — завысить безопаснее.
  ⚠️ Длина трубы берётся **сырая** (`RAWLENGTH`), НЕ `LENGTH`: в
  `LENGTH` сидят запасы на кабель, к трубе они отношения не имеют.
  ⚠️ ЭТАП 2.5 — отдельный скан слоя СУЩЕСТВУЮЩИХ опор (для «ПУ на
  существующую опору»), проверяет `EQUIPSTATUS` — статус ИМЕННО
  оборудования, не самой опоры.
- **`LEP-DATA.LSP`** — хранение данных объектов через XDATA.
  ⚠️ Номер нового маршрута берётся из САМОГО ЧЕРТЕЖА
  (`LEP:GetMaxRouteNumber`), не из счётчика в памяти — счётчик
  сбрасывался при перезапуске AutoCAD и молча сливал два маршрута в
  один блок ведомости.
  ⚠️ `LEP:AddEquipToExistingPole` дописывает поля поверх существующих;
  обычная `LEP:AddEquipData`/`LEP:AddPoleData` перезаписывает XDATA
  ЦЕЛИКОМ, включая `LABELHANDLE`. Не перепутать.
- **`LEP-KTPSYM.LSP`** — символ КТП (прямоугольник + треугольник
  трансформатора). Размер 3×5 м — прикидка.
- **`LEP-KKSYM.LSP`** — символ кабельного киоска (прямоугольник без
  значка: трансформатора внутри нет). Размер 1×2 м — прикидка.
  ⚠️ Слой у КТП и киоска ОДИН И ТОТ ЖЕ, различаются они по ключу
  `TYPE` в XDATA, а не по слою.
  ⚠️ Приборы учёта отдельными объектами не рисуются — только число
  (`PUCOUNT`), в ведомость идёт строкой «ПУ».
- **`LEP-TRANSSYM.LSP`** — маркер точки разрыва участка КЛ (кружок
  Ø0,5 м на непечатаемом слое). Ни подписи, ни строки в ведомости, ни
  статуса — это граница двух видов работ, а не материал.
  ⚠️ Это **БЛОК, а не голый `CIRCLE`**: `LEP:PickPole` требует
  `INSERT`, и этот инвариант защищает от попадания в пролёт случайной
  линии подосновы.
  ⚠️ Тип `"TRANS"` внесён ЯВНО в «наземный» список
  `LEP:GetOneEndReserve` — иначе маркер получил бы запас как опора,
  причём дважды.
  ⚠️ Ставить маркер на поворотах траншеи НЕ нужно — размеры по коленам
  рисуются сами (`LEP:CreateSpanDimensions`).
- **`LEP-PITSYM.LSP`** — символ котлована ГНБ (прямоугольник 2×1,5 м).
  Ставится САМ из `LEP-SPAN` на обоих концах прокола, отдельной команды
  нет (ни постановки, ни удаления — решение пользователя).
  ⚠️ **Котлован — свойство УЗЛА, а не пролёта**, дедупликация по
  `HOSTHANDLE` (хендл концевого блока), а не по расстоянию. Из этого
  одного правила выходят ОБА требования: параллельные кабели ГНБ висят
  на тех же концевых блоках → котлован один; в точке смены направления
  узел (маркер `TRANS`) один → котлован один.
  ⚠️ Длинная сторона нарисована вдоль оси X блока, поэтому угол при
  вставке — ПРЯМО азимут прокола, БЕЗ поправки на 90° (в отличие от
  `LEP:GetPoleRotation`, см. `LEP-END.LSP`).
  ⚠️ Слой `LEP-PIT` ПЕЧАТАЕМЫЙ — `LEP:SetLayerNoPlot` для него
  сознательно не вызывается, в отличие от трёх служебных слоёв рядом.
  ⚠️ Подпись ОДНА на оба типа («Рабочий/Приемный котлован») — программа
  не знает, где встанет буровая; лишнее слово стирают руками. Второй
  прокол в тот же узел не добавляет ни котлована, ни второй выноски.
- **`LEP-GEOPOINTS.LSP`** — зелёные кружки-ориентиры по GPS-точкам из
  GeoJSON, три команды в три шага (`EXTRACT` → ручная конвертация
  координат на стороннем сайте → `DRAW`, плюс `CLEAR`).
  ⚠️ Пересчёт WGS84 → МСК-47 СОЗНАТЕЛЬНО не делается в LISP: своя
  полиномиальная формула идеально легла на калибровочный участок и
  развалилась на трёх других. Локальная подгонка не переживает переезд.
  ⚠️ **Перестановка X/Y**: конвертер отдаёт «северная,восточная», а у
  чертежа X = восточная (~2 250 000), Y = северная (~450–460 тыс.).
  `LEP-GEOPOINTS-DRAW` делает перестановку явно.
  ⚠️ Количество строк координат и названий СВЕРЯЕТСЯ; при расхождении
  не рисуется ничего вообще, чтобы название не приклеилось к чужой
  точке.
  ⚠️ Файлы пишутся рядом с ЧЕРТЕЖОМ (`DWGPREFIX`), не рядом с geojson.
  ⚠️ Фон под подписью геоточки поставить НЕЛЬЗЯ: это `TEXT`, а фоновая
  маска — свойство `MTEXT`/`MULTILEADER`. Понадобится — менять тип
  объекта.
- **`LEP-UTILS.LSP`** — общие вспомогательные функции (округление,
  статус, меню выбора, работа с текстом `MULTILEADER`, фон под
  текстом, `DRAWORDER`).
- **`LEP-POLYLINE.LSP`** — `LEP:GetVertices`, грузится раньше всех.

**Перед добавлением новой функции** проверь, нет ли уже похожей в
`LEP-UTILS.LSP`/`LEP-POLYLINE.LSP` — дублирование в проекте уже
случалось (`LEP:GetVertexList`/`LEP:GetVertices` делали одно и то же).

## ⚠️ Осознанные решения, которые выглядят как баги

Не «чини» это — так задумано, и каждый пункт уже обсуждался:

- **`*LEP-KL-Reserve-Pole*` и `-Ground*` равны нулю.** Это не забытое
  значение: пользователь обнулил запас по просьбе коллеги («на чертеже
  хочу видеть длины без запаса»). Константы заведены на будущее.
- **Список МТП не фильтруется по классу напряжения маршрута** —
  пользователь хочет видеть все варианты, вдруг надо поставить МТП не
  того класса, что у трассы. А вот фолбэк на Enter классозависимый.
- **«Стойка СВ-110-5» в ведомости приходит из ДВУХ семейств опор**
  (6–20 кВ «20-3Н» и 0,4 кВ «21») и сливается в одну строку. Для
  закупки это верно — изделие физически одно.
- **Опоры общего участка остаются за маршрутом 1**, даже когда по ним
  идёт провод маршрута 2. Маршруту 2 там нужен только провод.
- **Название МТП пишется в XDATA строкой целиком** (ключ `EQUIP`), а
  не вычисляется заново из конфига при генерации отчёта. Иначе МТП
  разной мощности слились бы в одну строку ведомости.
- **Вершина маршрута под удалённой опорой НЕ убирается** — безвредный
  хвостик на непечатаемом слое, при повторной постановке в ту же точку
  переиспользуется.

## ⚠️ Уже начерченное задним числом не пересчитывается

Общее правило, повторявшееся уже раз пять: длины, типы, статусы и
запасы лежат в XDATA конкретных объектов. Правка кода или конфига
**не трогает то, что уже стоит на чертеже** — нужно перечертить.
Это касается и норм запаса, и округления, и типа опоры у ПУ, и
`LABELHANDLE` у опор из `LEP-END`. Говори об этом пользователю сразу,
когда меняешь что-то из этого списка, — иначе он проверит правку на
старом чертеже и решит, что она не работает.

## ⚠️ XDATA: значения ВСЕГДА строки

`LEP:SetData` собирает XDATA через `strcat` — он принимает только
строки. Число, переданное туда, роняет запись с `bad argument type`
**посреди работы**: XDATA не пишется ВООБЩЕ, а объект остаётся на
чертеже выглядящим нормально. Симптомов ноль — объект просто выпадает
из ведомости и из всех проверок по `TYPE`. Так КТП полтора дня жила
без единого байта XDATA.

**Делай `rtos`/`itoa` у себя.** В `LEP:SetData` есть страховка (числа
приводятся к строке), но она нужна лишь чтобы описка стоила формата
числа, а не потерянных данных.

⚠️ `nil` там СОЗНАТЕЛЬНО пропускает ключ целиком, а не пишет пустую
строку: в AutoLISP `""` — ИСТИНА, и проверки
`(if (LEP:GetDataValue E "KEY") ...)` по всему проекту начали бы
срабатывать на пустых данных.

## ⚠️ Слои: две ловушки, на которых уже обожглись

- **Всё, что рисуется КОМАНДОЙ (`_.MLEADER`, `_.PLINE`, `DIMALIGNED`),
  идёт на ТЕКУЩИЙ слой.** Такая функция ОБЯЗАНА сохранить `CLAYER`,
  выставить свой слой через `LEP:SetCurrentLayer` и вернуть старый в
  конце. На `entmake` это не распространяется — там слой задаётся
  группой 8 прямо в списке. На этом уже терялись подписи КТП и киоска
  (уезжали на непечатаемый слой и не попадали в PDF).
- **`LEP:CreateLayer` вызывает `-LAYER Make` только если слоя ещё нет,
  а `-LAYER Make` делает слой ТЕКУЩИМ.** Значит на уже начатом чертеже
  создастся ровно один слой — новый, только что добавленный в
  программу, — и текущим станет он. `C:LEP-SETUP` поэтому выставляет
  текущий слой ЯВНО последней строкой. **Добавляя новый слой, помни:
  на старых чертежах он попытается стать текущим.**

## ⚠️ Квадратные скобки в запросах: только список ответов

Dynamic Input (подсказка у курсора) строит выпадающий список вариантов
из **ПЕРВОЙ группы в квадратных скобках**, которую найдёт в тексте
запроса, — какой бы эта группа ни была. Наш собственный префикс
`[LEP-SPAN]` в строке `getkword` оказывался первым, и в списке у
курсора появлялся пункт «LEP-SPAN» вместо «Да»/«Нет».

**В консоли при этом всё выглядит правильно** — там текст печатается
как есть. Поэтому проверять такие вещи надо в самом AutoCAD, а не по
логу.

**Правило:** в строке запроса к `getkword`/`getreal`/`getpoint`
единственная группа в квадратных скобках — это СПИСОК ОТВЕТОВ.
Префиксы и пояснения — отдельным `princ` перед `initget`, либо без
скобок. `princ` ставить ДО `initget`, а не между `initget` и
запросом: `initget` действует на следующий запрос.

## ⚠️ MULTILEADER: коды повторяются на разных уровнях

У `MULTILEADER` одни и те же DXF-коды встречаются в
`CONTEXT_DATA{`/`LEADER{`/`LEADER_LINE{`. Обычный `assoc`/`subst` по
всему списку задевает чужие поля с тем же значением (поймано живым
тестом на группе 292). Правь только «голову» списка до маркера
`(302 . "LEADER{")` — так сделано в
`LEP:SetMLeaderBackgroundMask`.

Фон под текстом — свойство КАЖДОЙ отдельной сущности `MULTILEADER`
(группы 91/141/291/292), а НЕ стиля `MLEADERSTYLE`.

## Правила работы с кодом в этом проекте

- **⚠️ ЕСЛИ НЕ УВЕРЕН В ТОЧНОМ ПОВЕДЕНИИ/СИНТАКСИСЕ — СНАЧАЛА ПРОВЕРЬ
  МАЛЕНЬКИЙ КУСОЧЕК ЖИВЬЁМ В КОНСОЛИ AUTOCAD, ПОТОМ ПИШИ В ФАЙЛ.**
  Это касается: точного порядка аргументов `command` (уже ловили баги
  на `LEADER`, `-linetype`), единиц измерения углов (радианы/градусы —
  не одинаково для всех DXF-групп), точной структуры `entmake` для
  таблиц (`STYLE`/`LAYER` требуют маркеров подкласса), доступности
  шрифтов в конкретной установке AutoCAD. Если сам не можешь
  протестировать — дай пользователю МАЛЕНЬКИЙ изолированный кусок
  кода, попроси вставить в командную строку AutoCAD и показать
  результат, и только ПОСЛЕ этого пиши финальную версию в файл. Не
  полагайся на память о DXF-документации там, где реальный тест
  возможен и дёшев.
- ⚠️ В кусок для вставки в командную строку AutoCAD **не вставляй
  комментарии `;;`** — она их не понимает. Только рабочий код.
- Это AutoLISP (Visual LISP), а не обычный Lisp — `vl-*`, `vla-*`
  (ActiveX, только Windows), DCL-диалоги. При правках проверяй
  кроссплатформенность: **Mac не поддерживает ActiveX**.
- Предпочитай `entmake` интерактивным командам (`-BLOCK` и т. п.) —
  надёжнее и не зависит от состояния командной строки.
- Автоматических тестов нет, всё тестируется вручную через `APPLOAD`.
  После значимых правок обнови [LEP-README.md](LEP-README.md).
- Комментарии в коде и описания в README — максимально подробные,
  с объяснением ПОЧЕМУ именно так (см. «Что это за проект»).

## Открытые вопросы и риски

- **⚠️ НЕ ПРОВЕРЕНО ВЖИВУЮ (2026-08-11)**: допуск 10 см перед
  округлением вверх (`*LEP-Round-UpTolerance*`). Что проверить: размер
  с `Measurement` 50,0025 подписан «50 м», а не «51 м»; пролёт ~46,5 м
  по-прежнему даёт «47» и в выноске, и в размере; строка CSV сходится
  с выноской на том же пролёте; «Труба для ГНБ-прокола» не изменилась.
- **⚠️ НЕ ПРОВЕРЕНО ВЖИВУЮ (2026-08-11)**: котлованы ГНБ. Что проверить:
  обычный прокол даёт ДВА котлована вдоль трассы с отступом внутрь;
  второй параллельный кабель ГНБ по тем же опорам — НИ ОДНОГО нового;
  два прокола через маркер смены направления — в точке маркера ОДИН
  котлован и ОДНА подпись; траншея не даёт котлованов вообще; подпись
  ушла на `LEP-TEXT` и видна в PDF; короткий прокол (2–3 м) не даёт
  наезда котлованов друг на друга и ругается в консоль про ужатый
  отступ; в CSV про котлованы ни строки.
- **⚠️ НЕ ПРОВЕРЕНО ВЖИВУЮ (2026-08-05)**: три правки этого дня, каждая
  своим коммитом (откатываются по отдельности) — единое правило
  округления длин, посегментные размеры у ВЛ, защита от привязки к
  чужой вершине. Что проверить: пролёт ~46,5 м даёт **47** и в
  выноске, и в размере; короткое колено 1,1 м подписано «1 м»; строка
  «Труба для ГНБ-прокола» НЕ изменилась; обычный пролёт ВЛ между
  соседними опорами — ровно один размер, как раньше; клик «первая →
  последняя» даёт размер между каждой парой; пролёт по маршруту 2 на
  общем участке говорит «вершина добавлена» и достаёт до обеих опор;
  клик по объекту вдали от маршрута НИЧЕГО не рисует и громко ругается.
- **⚠️ НЕ ПРОВЕРЕНО ВЖИВУЮ (2026-08-04)**: размеры по коленам и
  объединение траншей через маркер. Что проверить: «молния» даёт
  размер на каждое колено; прямой пролёт — ровно один размер; две
  траншеи через маркер дают ОДНУ подпись; **два прокола ГНБ через
  маркер по-прежнему дают ДВЕ подписи и две запасные трубы** (правку
  03.08 легко задеть); у параллельных пролётов размеры не дублируются.
- **⚠️ НЕ ПРОВЕРЕНО ВЖИВУЮ (2026-08-03)**: стойки вместо типов опор в
  ведомости. Что проверить: в CSV нет строк «Опора …», вместо них
  «Стойка СВ-95-3»/«Стойка СВ-110-5»; количество сходится вручную
  (А23 = 2 стойки, УА23 = 3); символы опор на чертеже не изменились;
  неизвестный тип даёт строку «!!! НЕТ В СПРАВОЧНИКЕ СТОЕК».
- **⚠️ НЕ ПРОВЕРЕНО ВЖИВУЮ (2026-08-03)**: тип «П23» у окончания ПУ.
  Что проверить: подпись новой опоры ПУ — «Проект. оп. П23»; в CSV
  строка с префиксом «Опора П23»; **МТП и СТП по-прежнему «П20-3Н»**;
  символ опоры выглядит как раньше.
- **⚠️ НЕ ПРОВЕРЕНО ВЖИВУЮ (2026-08-03)**: два прокола подряд.
  Что проверить: два прокола через маркер дают ДВЕ подписи и ДВЕ
  запасные трубы в CSV; сценарий «ГНБ → маркер → траншея» не сломался;
  **обычная цепочка пролётов БЕЗ маркера по-прежнему объединяется в
  одну подпись** (правка стоит в `LEP:GetSpanNeighbors`, через которую
  проходит вообще всё объединение — её легко задеть).
- **⚠️ НЕ ПРОВЕРЕНО ВЖИВУЮ (2026-07-24)**: кабельный киоск, пролёт
  между наземными объектами, поконцовый запас, фикс параллельных
  близнецов, высокая сторона КТП, точка смены способа прокладки и
  фикс объединения подписей по `METHOD`.
- **⚠️ НЕ ПРОВЕРЕНО ВЖИВУЮ (2026-07-24)**: новые нормы запаса
  (`LEP:GetRealLength`/`LEP:GetPhaseCount`), разделение «чертёж vs
  ведомость» (`LEP:GetDisplayLength`) и подсчёт труб ГНБ (ЭТАП 1.7).
  Что проверить: подписи на чертеже НЕ изменились; ВЛ 10 кВ в
  ведомости ≈ втрое больше, чем в подписи; появилась строка «Труба для
  ГНБ-прокола (13 м)».
- **⚠️ ОТКРЫТЫЙ ВОПРОС К КОЛЛЕГЕ**: подтверждено только **А23 = 2
  стойки**. УП23 (2) и УА23 (3) взяты по аналогии из той же серии
  3.407.1-143 — переспросить.
- **⚠️ Марки СИП-3** в `*LEP-WireTypes-VL-10*` введены «на
  правдоподобный вид» по референсу, с реальным каталогом не сверены.
  Марки для 6 кВ скопированы с 10 кВ и пользователем не подтверждены.
- **⚠️ Цвет слоя для 6 кВ** сознательно не свой — переиспользует
  10 кВ (решение пользователя, «дальше видно будет»).
- **⚠️ Фон под текстом при ПЕЧАТИ не проверен** ни у выносок, ни у
  размерных линий. Если вылезет тёмный прямоугольник — `DIMTFILL` = 2
  плюс явный белый `DIMTFILLCLR`.
- **⚠️ Размер стрелки `MLEADER` 0.8** визуально на реальном чертеже не
  сверен (в отличие от `DIMASZ` 0.8, который сверяли).
- **⚠️ Геоточки**: парсер GeoJSON подтверждён живым тестом, но
  перестановка X/Y и привязка кружков к реальной подоснове визуально
  ещё не сверены. Рекомендация — сверить хотя бы одну характерную
  точку после `LEP-GEOPOINTS-DRAW`.
- **⚠️ Группы DXF 174/178/295/298** (подчёркивание текста выноски)
  подтверждены только КОМБИНАЦИЕЙ, по отдельности не проверялись.
- **⚠️ Не проверено, что `getreal` гарантированно игнорирует клик
  мышкой** (ввод расстояния до следующей опоры в `LEP-POLE`). Это
  стандартное поведение AutoLISP, но именно здесь не тестировалось.
- **⚠️ Заливка символа опоры (`SOLID`)** — подтверждена рабочим тестом
  в консоли (порядок вершин 1-2-4-3, не по периметру), но в
  `LEP-POLESYM.LSP` не интегрирована: отложено по решению
  пользователя.
- **⚠️ После одного прогона `LEP-SCALE`** у пользователя оказались
  выключены два слоя. Код-ревью не нашло в проекте механизма, способного
  это сделать. Не воспроизвелось, причина не найдена — если повторится,
  см. README.

## Режим подтверждения правок

Перед любой работой сначала пиши план: что нашёл/понял и что
собираешься менять и почему. Дальше — по ситуации:

- Если правка однозначно вытекает из того, что показал сам
  пользователь (лог ошибки, скриншот, описание бага) и реального
  выбора нет — делай сразу, без паузы на подтверждение, и отчитайся,
  что сделал.
- Если есть настоящая развилка (несколько разумных вариантов
  реализации, дизайн-решение, что-то не тривиально отменить/легко
  сделать зря) — остановись и спроси, какой вариант нужен, ПЕРЕД тем
  как редактировать файлы.
- Не удаляй непонятное молча: сначала спроси, потом чини. Уже был
  случай, когда осознанная вставка пользователя была принята за мусор.
- Если `LEP-CONFIG.LSP` не совпадает с тем, что записано здесь или в
  README, — спроси пользователя, не правь молча.
