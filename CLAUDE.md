# CLAUDE.md

Инструкции для Claude Code в этом репозитории.

## Что это за проект

Набор AutoLISP-модулей (`LEP-*.LSP`) для автоматизации черчения ЛЭП
(линий электропередач) в AutoCAD 2025 — трассировка маршрута,
расстановка опор, нарезка пролётов, выгрузка ведомости.

Источник правды по логике работы, текущему состоянию и известным
проблемам — [LEP-README.md](LEP-README.md). Обновляй его при каждом
значимом изменении в модулях.

## Правило хранения файлов (✅ СНЯТО 2026-07-16)

**Раньше было критическим правилом**: DWG чертежа должен лежать в той
же папке, что и модули `LEP-*.LSP` — на этом держался поиск файлов
(`findfile`) и регистрация `TRUSTEDPATHS`. **Больше не актуально**:
папка с модулями добавлена в Support File Search Path (Options →
Files → Support File Search Path) — разовая настройка НА КОМПЬЮТЕР
(не на чертёж), после нее `findfile` находит модули независимо от
того, где физически лежит DWG (подтверждено живым тестом 2026-07-16 -
`findfile` нашёл файл модуля из чертежа, сохранённого в другой
папке). `LEP-START.LSP` тоже больше не берёт путь для `TRUSTEDPATHS`
из папки чертежа (`DWGPREFIX`) — берёт СВОЮ СОБСТВЕННУЮ папку через
`(findfile "LEP-START.LSP")`. Если у нового пользователя (например, у
коллеги) Support Path ещё не настроен — `LEP-START.LSP` явно
предупредит об этом при загрузке, не будет тихо ломаться.

## Структура модулей

- `LEP-START.LSP` — загрузка всех модулей через Support Path/findfile,
  авторегистрация СВОЕЙ папки в TRUSTEDPATHS (через
  `(findfile "LEP-START.LSP")`, не через папку DWG — см. выше). **✅
  2026-07-16**: теперь загружается АВТОМАТИЧЕСКИ при открытии любого
  чертежа через `acaddoc.lsp` (зарезервированное имя файла, штатный
  механизм автозагрузки AutoCAD — есть и на Mac, и на Windows) — не
  нужно вручную вызывать `APPLOAD` каждый раз. Ручной
  `APPLOAD`/`(load "LEP-START.LSP")` по-прежнему работает — удобно
  для перезагрузки кода после правок без переоткрытия чертежа. ⚠️
  `acaddoc.lsp` — зарезервированное имя, AutoCAD грузит ТОЛЬКО ПЕРВЫЙ
  найденный во всём Support Path: если когда-нибудь появится другой
  плагин с таким же файлом — придётся объединять вызовы в одном
  файле. Проверено перед созданием: `(findfile "acaddoc.lsp")` вернул
  `nil`, конфликтов не было. После загрузки всех модулей автоматически
  вызывает `(C:LEP-SETUP)` — слои проекта создаются без отдельной
  ручной команды (`LEP:CreateLayer` безопасен при повторном вызове,
  ничего не пересоздаёт). Команда `LEP-SETUP` больше не значится в
  списке команд, который печатается после загрузки — она не нужна для
  обычной работы (осталась доступна вручную на случай, если слой
  случайно удалили).
- `LEP-CONFIG.LSP` — все настраиваемые параметры проекта (блок опоры,
  геометрия символа-трапеции, типы проводов/кабелей по классу
  напряжения, режим диалога DCL/консоль и т.д.).
- `LEP-SCALE.LSP` — калибровка масштаба подложки по двум точкам.
- `LEP-ROUTE.LSP` — черчение маршрута (полилиния), присвоение ID
  (`LEP_ROUTE_01`, ... — 2 цифры, см. `LEP:CreateRouteID` в
  LEP-DATA.LSP). Сразу ставит подпись "Маршрут NN" у начальной точки
  (`LEP:CreateRouteLabel`, с 2026-07-15 через `_.MLEADER`, было
  `_.LEADER` — см. README), иначе трассу снаружи не отличить, ID
  виден только в XDATA. **✅ 2026-07-15**: подпись переехала на слой
  `*LEP-Layer-Route*` (была на `*LEP-Layer-Text*`) — чисто техническая
  пометка, печать там всегда выключена (`LEP:SetLayerNoPlot`), не
  попадает в PDF; побочный эффект — теперь скрывается/показывается
  вместе со служебной линией маршрута через `LEP-ROUTEVIS`, не видна
  всегда, как раньше (обсуждено с пользователем, это и есть нужное
  поведение). Спрашивает класс напряжения маршрута (10кВ/
  0,4кВ, `*LEP-VoltageClasses*`) ОДИН РАЗ на весь маршрут — хранится в
  XDATA (`VCLASS`), используется в `LEP-SPAN` (список марок провода) и
  `LEP-POLE` (вторая строка подписи опоры). Рисуется на служебном слое
  `*LEP-Layer-Route*` (НЕ на слое ВЛ) — команда `LEP-ROUTEVIS` (тоже
  в этом файле) переключает его видимость на экране; печать слоя
  выключена всегда, отдельно от видимости (см. `LEP-SETUP.LSP`).
- `LEP-POLESYM.LSP` — символ опоры (трапеция 1×1м с узкой стороной
  0,7м + крест 1,07м через центр, крест ФИКСИРОВАННО серый —
  `*LEP-Color-PoleCross*`, задан прямо в геометрии блока, группа DXF
  62 — не зависит от цвета/статуса самой опоры). Создаётся автоматически
  из `LEP-START.LSP` через `entmake` (BLOCK/LWPOLYLINE/LINE/ENDBLK) —
  НЕ через интерактивный `-BLOCK`, надёжнее (см. правило про `command`
  ниже). **⚠️ Блок создаётся ТОЛЬКО если его ещё нет** — в отличие от
  слоёв, НЕ передефайнится на каждой загрузке (честный передефайн
  `-BLOCK` не протестирован вживую в консоли). Толщина линии по
  статусу (Проект. жирнее Сущ.) — через `LWEIGHT` (DXF 370) на самом
  `INSERT`, геометрия внутри блока имеет `LWEIGHT = ByBlock`.
  Разворот — по направлению маршрута (широкая сторона к началу трассы,
  узкая к концу). По типу опоры (`*LEP-PoleColumns*`) собирает 1/2/3
  трапеции (главная опора + откос(ы) для анкерных/угловых типов, шаг
  `*LEP-Pole-Sym-Offset*` = 2м) — **⚠️ угол разворота откоса — черновая
  прикидка, не подтверждена**, см. README (раздел с восклицательными
  знаками).
- `LEP-SETUP.LSP` — создаёт все слои проекта с цветами из CONFIG (цвет
  проектируемых опор — оранжевый, `*LEP-Color-Proj-VL*`, был красный —
  совпадал со служебной линией маршрута). Запускается автоматически из
  `LEP-START.LSP`, руками вызывать не нужно (иначе, если бы отдельная
  команда была забыта, `LEP:SetCurrentLayer` не нашёл бы слой и
  промолчал, объект ушёл бы не на тот слой). Последним создаётся слой
  `*LEP-Layer-Manual*` (`LEP-MANUAL`) — СОЗНАТЕЛЬНО последним:
  `-LAYER Make` всегда делает только что созданный слой текущим
  (свойство самой команды AutoCAD, не обойти), поэтому пусть после
  автозапуска текущим остаётся безопасный "черновой" слой, а не
  случайный технический (ВЛ/КЛ/опоры/DIM), куда могла бы случайно
  попасть ручная линия и испортить ведомость. Также создаёт единый
  текстовый стиль проекта (`LEP:CreateTextStyle` — наклонный
  `ISOCPEUR`, угол 10°, высота 1,3, делается текущим и для текста, и
  для размеров `DIMTXSTY`) — см. README для пояснения, почему высота
  у текстового стиля автоматически замещает `DIMTXT` у размерных
  линий. `entmake` для таблицы `STYLE` требует маркеры подкласса
  (`AcDbSymbolTableRecord`/`AcDbTextStyleTableRecord`) - без них
  запись формально создаётся, но `setvar "TEXTSTYLE"` её отклоняет
  ("AutoCAD variable setting rejected"); сам `setvar` для
  `TEXTSTYLE`/`DIMTXSTY` обёрнут в `vl-catch-all-apply`, чтобы сбой
  тут не обрывал остаток `C:LEP-SETUP`. **⚠️ Проверено вживую в
  консоли** (не по памяти): `"ISOCPEUR"` (БЕЗ `.shx`!) на проверенной
  установке — TrueType-шрифт, не SHX-файл (SHX-аналог с похожим видом —
  `"isocp.shx"`, без "eur"); угол наклона (группа DXF 50 у `STYLE`) — в
  РАДИАНАХ, не в градусах, как изначально предполагалось по памяти
  (`*LEP-TextStyle-ObliqueAngle*` в CONFIG хранится в градусах,
  перевод в радианы — в `LEP:CreateTextStyle` перед `entmake`).
  Также настраивает стиль выносок `MLEADER` (`LEP:CreateMLeaderStyle` —
  правит НАПРЯМУЮ стиль "Standard" через `dictsearch`/`entget`/`subst`/
  `entmod`, т.к. `MLEADERSTYLE` не классическая таблица AutoCAD и
  `tblobjname` для него возвращает `nil`) — миграция подписей с
  `LEADER` на `MLEADER` от 2026-07-15, полная история и найденные
  DXF-группы (43/42/44/45/93/174/178/295/298) — в README.
- `LEP-LINETYPES.LIN` — УДАЛЁН ИЗ ПРОЕКТА (2026-07-15). Содержал
  кастомный тип линии `LEP_CABLE` для КЛ (штамп "K" в разрывах) —
  после серии живых тестов (поворот буквы, размер, разрыв вокруг неё)
  пользователь решил убрать эту линию совсем. КЛ теперь рисуется
  `CONTINUOUS`, как и ВЛ, отличается только слоем/цветом. См. историю
  git, если понадобится восстановить.
- `LEP-POLE.LSP` / `LEP-POLE.DCL` — ручная расстановка опор в цикле:
  проекция на трассу, вставка вершины, выбор типа/статуса, вставка
  символа-трапеции (`LEP-POLESYM.LSP`, разворот по маршруту), привязка
  ГЛАВНОЙ трапеции через `GEOMCONSTRAINT` (откосы не привязаны),
  двухстрочная подпись-выноска ("[статус]. оп. [тип]" + класс линии
  маршрута, с 2026-07-15 через `_.MLEADER` + код разрыва абзаца `\P`
  для второй строки, было `_.LEADER` — см. README).
  `LEP:InsertVertexOnRoute` (общая с `LEP-END.LSP`) НЕ
  вставляет вторую вершину, если клик попал в пределах
  `*LEP-Vertex-Snap-Tolerance*` от уже существующей (исправленный баг:
  угловая опора на реальном изломе маршрута создавала вырожденный
  отрезок и роняла `GEOMCONSTRAINT` конфликтом "over-constrain"). После
  каждого `GEOMCONSTRAINT` идёт пустой `(command)` (Escape) — страховка
  на случай, если констрейнт всё же не наложится по другой причине,
  чтобы команда не зависала и не портила остаток цикла. **✅ 2026-07-15**:
  `LEP:PromptNextPolePoint` — начиная со 2-й опоры расстояние от
  предыдущей можно ВВЕСТИ числом СО ЗНАКОМ (`getreal` — клик мышкой
  игнорируется, ждёт клавиатуру), точка ставится вдоль трассы через
  `LEP:GetPointAtDistance` (`LEP-POLYLINE.LSP`, экстраполирует прямо за
  пределами трассы, как МТП/ЩУ в `LEP-END`). Знак = направление: "+"
  от подписи "Маршрут NN" (первая вершина маршрута) к дальнему концу,
  "-" обратно к ней — бесплатный ориентир, готовой "стрелочной" линии
  в AutoCAD нет (проверено), самодельная была бы той же мороки, что
  убранная `LEP_CABLE`. **⚠️ ФИКС того же дня**: первая версия
  угадывала направление ПО ПОЗИЦИИ первой опоры (к какой вершине
  маршрута ближе) — ломалось у СЕРЕДИНЫ трассы (сравнение сводилось к
  случайным долям миллиметра, направление непредсказуемо). Знак убрал
  угадайку и всё связанное с ней состояние (`Forward`/`ForwardKnown`)
  целиком — теперь направление задаётся явно каждый раз, ничего не
  запоминается между опорами. Enter вместо числа — клик мышкой с
  базовой точкой (родная резинка AutoCAD). **✅ ФИКС 2026-07-15**:
  подсказка Dynamic Input (расстояние/угол у курсора), на которую
  рассчитывали — НЕ появляется у голого `getpoint` из LISP (проверено
  пользователем вживую: резинка есть, числа нет) — похоже, это фишка
  только штатных команд AutoCAD (`LINE` и т.п.). Вместо неё — эхо
  фактического расстояния СРАЗУ ПОСЛЕ клика (не во время
  протаскивания, но без ручного замера).
  ⚠️ Не проверено вживую, что `getreal` гарантированно игнорирует клик
  мышкой — стандартное поведение AutoLISP, но не тестировано именно
  здесь. Там же `LEP-POLE-ALONE` — та же опора БЕЗ привязки к
  трассе (свободно двигается, без разворота — нет маршрута, от
  которого его считать), но для ведомости автоматически привязывается
  к БЛИЖАЙШЕЙ трассе по ID (`LEP:FindNearestRoute`, теперь возвращает
  ещё и класс напряжения этой трассы для подписи) — **⚠️ известный
  риск неверной привязки на границе территорий двух заявителей, см.
  README** (раздел с восклицательными знаками вверху файла).
  **✅ `LEP-POLE-DELETE`/`LEP-POLE-MOVE` (2026-07-15)**: опора = блок +
  отдельный `MLEADER`-подпись + откосы (у анкерных/угловых) без единой
  связи между ними в AutoCAD — простое стирание блока оставляло
  подпись висеть в воздухе (живым тестом подтверждено: `entdel` блока
  НЕ трогает подпись) и откосы сиротами. `LEP:DeletePoleGroup` убирает
  все три: подпись — по хендлу из XDATA (`LABELHANDLE`,
  `LEP:AddPoleData`), откосы — по признаку "тот же блок рядом, но БЕЗ
  XDATA" (у откосов её нет вообще). Вершину трассы НЕ трогаем -
  безвредный хвостик на служебном слое. `LEP:PlacePoleOnRoute` -
  вставка одной опоры вынесена из `C:LEP-POLE` в общую функцию, её же
  использует `LEP-POLE-MOVE` (читает тип/статус старой опоры из XDATA
  перед удалением, переспрашивать не нужно).
- `LEP-END.LSP` — оконечное оборудование маршрута: ПУ (0,4кВ, 1 опора
  `*LEP-End-PoleType*` + подпись "ПУ") или МТП (10 **или 6** кВ, 3 такие
  же опоры подряд, шаг 3м/2м, подписи РЛК/МТП/ЩУ — мощность И класс
  высокой стороны выбираются одной строкой из `*LEP-MTP-Types*`
  (`LEP-CONFIG.LSP`), см. "Известные открытые проблемы" ниже.
  **✅ 6 кВ добавлено 2026-07-24** — две новые строки в списке
  ("МТП-160/6/0,4"/"МТП-250/6/0,4"), больше НИЧЕГО менять не
  пришлось: геометрия блока, подписи РЛК/ЩУ, углы разведения, слой и
  статус от класса напряжения не зависят, а ведомость и так пишет
  выбранную строку целиком в `EQUIP`. ⚠️ Список СОЗНАТЕЛЬНО не
  фильтруется по `VCLASS` маршрута (решение пользователя: видеть все 4
  варианта, вдруг надо поставить МТП не того класса, что у трассы) —
  но фолбэк на Enter теперь классозависимый, `LEP:GetDefaultMtpLabel`
  ищет в списке первую строку с "/[класс]/", иначе откат на
  `*LEP-Label-MTP*`: раньше там жёстко стояло 10 кВ и случайный Enter
  на 6-киловольтной трассе молча дал бы неверную строку ведомости).
  ОДНА концовка за
  запуск, без цикла (просьба пользователя). РЛК/МТП/ЩУ/ПУ — не
  своя графика, только текстовые подписи. Три подписи МТП разведены в
  разные стороны (`*LEP-End-LabelAngle-RLK*`/`-MTP*`/`-SchU*`) - опоры
  стоят близко (шаг 3м/2м), при одинаковом направлении подписи
  накладывались друг на друга. Первая опора — как в `LEP-POLE`
  (проекция+вершина+`GEOMCONSTRAINT`), опоры 2/3 МТП — дальше ПО УГЛУ
  МАРШРУТА (RouteDir), без проекции. **⚠️ Угол маршрута и угол
  поворота блока (`LEP:GetPoleRotation`) — РАЗНЫЕ углы, отличаются на
  90° — их уже один раз перепутали (опоры МТП уезжали перпендикулярно
  трассе, фикс 2026-07-11), не путать снова.** Тип опоры зафиксирован,
  не спрашивается. **✅ ФИКС 2026-07-15**: статус (Проект./Сущ.) теперь
  спрашивается (`LEP:GetStatus`, один раз на блок) — раньше был зашит
  в "Проект.". **✅ ФИКС 2026-07-15**: RouteDir разворачивается на 180°,
  если точка установки ближе к ПЕРВОЙ вершине маршрута, чем к
  ПОСЛЕДНЕЙ (`LEP:GetVertices`) — раньше опоры 2/3 корректно уезжали
  наружу только с последней вершины, с первой налезали на трассу.
  **✅ ТРЕТИЙ ПУНКТ 2026-07-15 — "ПУ на существующую опору"** (не
  заводили отдельную команду - "команд и так хватает"): выбор типа
  теперь ПЕРВЫМ вопросом (маршрут этому пункту не нужен вообще). Клик
  по существующей опоре → `LEP:AppendMLeaderText` (`LEP-UTILS.LSP`)
  дописывает "+ проект. ПУ" прямо в её подпись (группа DXF 304 - живой
  тест подтвердил, что коллизии со вторым, служебным смыслом той же
  группы глубже в структуре MULTILEADER нет) - никакой новой
  трапеции/вершины/`GEOMCONSTRAINT`. Данные - `LEP:AddEquipToExistingPole`
  (`LEP-DATA.LSP`), НЕ обычная `LEP:AddEquipData` (та стёрла бы
  XDATA опоры целиком, включая `LABELHANDLE`) - пишет `EQUIP`/
  `EQUIPSTATUS` поверх уже существующих полей. См. `LEP-REPORT.LSP`
  ЭТАП 2.5 - отдельный скан слоя существующих опор, без него это
  оборудование не попало бы в CSV.
- `LEP-SPAN.LSP` — классификация пролётов между опорами (клик по
  блокам), выбор ВЛ/КЛ и марки провода/кабеля (список ВЛ зависит от
  класса напряжения маршрута — `*LEP-WireTypes-VL-10*` СИП-3 vs
  `*LEP-WireTypes-VL-04*` СИП-2, это разные провода физически),
  создание отдельного объекта-пролёта со своим слоем/типом линии. Длина материала (не то
  же самое, что расстояние между опорами!) считается с запасом.
  **✅ ПЕРЕПИСАНО 2026-07-24** — одна общая формула
  `(база + фикс.добавка) x (1 + процент) x число_фаз`
  (`LEP:GetRealLength`, теперь 4 аргумента: тип, способ прокладки,
  класс напряжения, длина): ВЛ — `*LEP-Sag-VL*` 4,5% на провис (было
  2%); КЛ — `*LEP-KL-Reserve*` 6 м на подъём на опору ПЛЮС свой
  процент по способу прокладки (`*LEP-Sag-KL-GNB*` 2% /
  `*LEP-Sag-KL-Trench*` 4%) — **проценты идут ПОВЕРХ метров, а не
  вместо**: метры — реальная физическая длина кабеля у концов пролёта,
  процент — запас на укладку, разные величины. **✅ 2026-07-24 метры
  стали ПОКОНЦОВЫМИ** (`LEP:GetEndReserve`): опора —
  `*LEP-KL-Reserve-Pole*` 3 м (подъём), наземные КТП/КК —
  `*LEP-KL-Reserve-Ground*` **0 м** (осознанный ноль, константа
  заведена на будущее по прямому решению пользователя). Отсюда
  опора-опора = 6 м как было, КТП-опора = 3 м, КТП-киоск = 0 м.
  Готовое число пишется в XDATA пролёта (`RESERVE`) при создании —
  ⚠️ и его ОБЯЗАТЕЛЬНО надо переносить в `LEP:SetSpanLabelHandle`/
  `LEP:ReassignSpanRoute`, иначе оно стирается сразу же
  (`LEP:SetData` перезаписывает XDATA целиком). Неизвестный `TYPE`
  трактуется как опора, а не как ноль; провод 10 кВ и 6 кВ
  дополнительно **умножается на 3** (`LEP:GetPhaseCount`,
  `*LEP-Phases-ByClass*`) — у СИП-3 на каждую фазу отдельный провод,
  ⚠️ к КЛ множитель НЕ применяется даже на 10 кВ (кабель трёхжильный =
  одна строительная длина). Округление вверх до метра — **один раз, в
  самом конце, ПОСЛЕ умножения на фазы** (иначе трижды докидывался бы
  хвостик округления). **⚠️ НЕ ПУТАТЬ ДВЕ ФУНКЦИИ ДЛИНЫ** (разведены
  окончательно 2026-07-24 по прямому указанию пользователя: "кабель и
  провод на чертеже показываешь без запаса (только подъём +6
  остаётся), все запасы идут только в ведомость"):
  `LEP:GetRealLength` → XDATA `LENGTH` → **ведомость** (все запасы +
  фазы); `LEP:GetDisplayLength` → **подпись на чертеже** (только
  физика: ВЛ сырая длина, КЛ сырая + 6 м на каждый пролёт группы,
  никаких процентов и никакого ×3 — иначе "95 м" на 30-метровом
  пролёте выглядело бы как ошибка черчения). `LEP:FinalizeSpanLabel`
  теперь НЕ заглядывает в `LENGTH` вообще, обе величины подписи
  считает от `RAWLENGTH`; округление до метра — один раз на всю группу
  пролётов, а не у каждого отдельно. Полный разбор — в README, раздел
  "Новые нормы запаса длины". Именно
  это число идёт в подпись и в XDATA `LENGTH`/ведомость (подпись
  пролёта — с 2026-07-15 через `_.MLEADER`, было `_.LEADER` — см.
  README). Отдельно рисуется размерная линия (`LEP:CreateSpanDimension`, слой
  `LEP-DIM`, команда `DIMALIGNED` — параллельно самому пролёту, а не
  всегда горизонтально/вертикально как у `DIMLINEAR`) с прямым
  расстоянием между опорами, округлённым по обычным правилам
  математики. **✅ ФИКС 2026-07-15**: текст размера теперь реально
  поворачивается вместе с линией и садится НАД ней (`DIMTIH`/`DIMTOH`/
  `DIMTAD` в `C:LEP-SETUP`) — старая формулировка этого пункта
  ("сейчас поворачивается") была неверна, эти переменные раньше не
  трогались вообще, держался дефолт AutoCAD (текст всегда
  горизонтален).
- `LEP-REPORT.LSP` — выгрузка ведомости в CSV. Без ActiveX (данные
  берутся из XDATA, не из блока/vla-*). Считает только проектируемые
  (Проект.) объекты, блоками по трассам + сводка "ИТОГО".
  **✅ ЭТАП 1.7 (2026-07-24) — труба для ГНБ-прокола**, отдельная
  позиция в штуках, никак не связанная с метражом самого кабеля:
  `(вверх(длина_прокола / *LEP-GNB-PipeLength*) + *LEP-GNB-PipeSpare*)
  x *LEP-GNB-PipesPerBore*` = `(вверх(L/13) + 1) x 2`. Свой отдельный
  `ssget` по слою `*LEP-Layer-Proj-KL-GNB*` (сам факт нахождения на
  этом слое = "это ГНБ и это проект"). Длина берётся **сырая**
  (`RAWLENGTH`, геометрия по плану), НЕ `LENGTH` — в `LENGTH` уже сидят
  +6 м и процент на кабель, к длине трубы это отношения не имеет.
  ⚠️ **Один прокол = сплошной участок ГНБ, а не один пролёт** — иначе
  прокол из двух пролётов дал бы две лишние запасные трубы. Признак
  "один прокол" — общая подпись (`LABELHANDLE`): соседние однородные
  пролёты и так объединяются под одной подписью
  (`LEP:FinalizeSpanLabel`) ровно по нужным условиям, готовая
  группировка уже лежит в чертеже. Копит длины `LEP:AddToBoreList`
  (считать трубы пролёт за пролётом нельзя — сначала надо собрать весь
  прокол). Нет `LABELHANDLE` → ключ = собственный хендл пролёта,
  прокол сам по себе (лучше завысить, чем потерять). Опоры
  `LEP-END` считаются дважды: как обычная опора по `MODEL` (уже было),
  и ОТДЕЛЬНОЙ строкой по ключу `EQUIP` (РЛК/МТП/ЩУ/ПУ) — **✅ ЭТАП 2.5
  (2026-07-15)**: отдельный скан слоя СУЩЕСТВУЮЩИХ опор (не только
  проектируемых) — для "ПУ на существующую опору" (`LEP-END.LSP`),
  проверяет ключ `EQUIPSTATUS` (статус ИМЕННО оборудования, не самой
  опоры), саму опору материалом не считает, только строку EQUIP.
  `LEP:GetEquipLabel` вынесена отдельной функцией — используется в
  обоих сканах, без дублирования. Префикс
  "Опора " в CSV теперь добавляется только реальным опорам (проверка
  по `*LEP-PoleTypes*`), а не всему подряд с `Unit="шт."`, иначе
  "РЛК" превращалось бы в "Опора РЛК". CSV в
  UTF-8 с BOM — ключ: `(open path "w" "utf8-bom")`, третий аргумент
  `open` (AutoCAD 2021+). Без него `open` пишет старой однобайтовой
  кодировкой (Windows-1251 для кириллицы); просто `"utf8"` без BOM
  не годится — Excel без метки BOM не определяет UTF-8 сам и
  показывает кракозябры, хотя другие программы открывают верно и без
  неё. Если где-то ещё в проекте понадобится писать файл с
  кириллицей — используй `"utf8-bom"` сразу, не по одной догадке.
- `LEP-DATA.LSP` — хранение данных объектов через XDATA,
  `LEP:GetDataValue` для чтения по ключу. `LEP:AddRoute` теперь пишет
  и класс напряжения (`VCLASS`). `LEP:AddEquipData` — то же, что
  `LEP:AddPoleData`, плюс ключ `EQUIP` для оконечного оборудования.
  Номер нового маршрута берётся из САМОГО ЧЕРТЕЖА
  (`LEP:GetMaxRouteNumber` — max существующих ID + 1), НЕ из счётчика
  в памяти — счётчик сбрасывался при перезапуске AutoCAD и ID
  дублировались, молча сливая два маршрута в один блок ведомости
  (фикс 2026-07-11). `LEP:AddPoleData` принимает статус параметром
  (раньше всегда писала PROJECT, даже для Сущ.).
- `LEP-KKSYM.LSP` — НОВЫЙ МОДУЛЬ (2026-07-24). Блок-символ кабельного
  киоска (КК) — **простой прямоугольник БЕЗ значка внутри** (в киоске
  трансформатора нет, в отличие от КТП с её треугольником), устроен по
  образцу `LEP-KTPSYM.LSP`. ⚠️ Размер 1×2 м — прикидка, как было у КТП
  с 3×5. Слой — ТОТ ЖЕ `*LEP-Layer-Proj-TP*`/`*LEP-Layer-Exist-TP*`,
  что у КТП: различаются они по ключу `TYPE` в XDATA, а не по слою
  (проверка `TYPE` в ЭТАПЕ 2.6 ведомости стояла с самого начала,
  поэтому киоск встал на готовый слой без правок в `LEP-SETUP`).
  Понадобился для схемы «КТП → два кабеля 0,4 кВ → киоск с двумя ПУ»,
  где опор нет вообще. Приборы учёта отдельными объектами НЕ рисуются
  (на плане внутри киоска они неразличимы) — только число (`PUCOUNT`),
  в ведомость идёт строкой «ПУ» в штуках тем же `*LEP-Label-PU*`, что
  и ПУ на опоре (один и тот же прибор, для закупки важно общее
  количество).
- `LEP-UTILS.LSP` — общие вспомогательные функции.
- `LEP-POLYLINE.LSP` — работа с полилиниями. Сейчас одна функция,
  `LEP:GetVertices` (список вершин LWPOLYLINE) — общая для ROUTE/
  POLE/SPAN/END, грузится раньше всех.
- `LEP-MAKETESTBLOCK.LSP` — УДАЛЁН ИЗ ПРОЕКТА (2026-07-10). Создавал
  старую заглушку `test-block` (кружок+атрибут) — полностью заменена
  настоящим символом-трапецией (`LEP-POLESYM.LSP`), файл больше не
  нужен даже для отладки (см. историю git, если понадобится).
- `LEP-GEOPOINTS.LSP` — НОВЫЙ МОДУЛЬ (2026-07-20). Ставит временные
  зелёные кружки-ориентиры (Ø1м, слой `LEP-GEOPOINTS`, непечатаемый —
  тот же принцип, что и у `*LEP-Layer-Route*`) в точках, где по GPS
  (полевой замер, экспорт в GeoJSON) должны стоять опоры — дальше
  пользователь рядом расставляет настоящие символы опор вручную. Три
  команды в три шага, а не одна: перевод координат WGS84 → МСК-47
  (местная система координат Ленобласти, зона 2, официальный код
  EPSG:6334702) — это полноценная геодезическая трансформация, НЕ
  сделана в LISP (первая идея — своя формула, полиномиальная регрессия
  по 25 калибровочным точкам одного участка — идеально легла на ЭТИ
  точки, но пользователь проверил на трёх разных зонах и убедился, что
  у каждой своё смещение — локальная подгонка не переживает переезд на
  другой участок; см. README, раздел "GPS-точки/GeoJSON", полная
  история). Вместо этого пользователь сам считает координаты через
  проверенный сайт-конвертер (официальный код EPSG, не самоделка), а
  LISP только читает готовый результат:
  1. `LEP-GEOPOINTS-EXTRACT` — ЕДИНСТВЕННЫЙ ручной выбор файла во всей
     цепочке (`getfiled` на geojson) → собственный узкий разбор
     структуры (`vl-string-search`/`substr`, НЕ полноценный JSON-парсер —
     в AutoLISP нет ни regex, ни готовой JSON-библиотеки) → три файла
     **рядом с ЧЕРТЕЖОМ** (`DWGPREFIX`, ✅ ФИКС 2026-07-20 по просьбе
     пользователя — раньше писались рядом с geojson, могла оказаться
     произвольная папка типа Downloads): `geopoints-coords.txt` (только
     "широта,долгота", БЕЗ названий — специально, чтобы весь файл можно
     было скопировать в конвертер одним действием), `geopoints-names.txt`
     (только названия точек, в ТОМ ЖЕ порядке строк) и ПУСТОЙ
     `geopoints-converted.txt` (создаётся только если его ещё нет — при
     повторном EXTRACT уже вставленный пользователем результат
     конвертера не затирается).
  2. (ручной шаг пользователя) — координаты → в конвертер → результат
     вставить в уже готовый `geopoints-converted.txt` и сохранить.
  3. `LEP-GEOPOINTS-DRAW` — ✅ ФИКС 2026-07-20: БЕЗ диалога выбора файла
     вообще, сама читает координаты + названия из `DWGPREFIX` по
     фиксированным именам. СВЕРЯЕТ количество строк (при расхождении —
     громкая ошибка, НЕ рисует ничего вообще, чтобы название не
     приклеилось к чужой точке), рисует круг+подпись. **⚠️ Перестановка
     X/Y** — конвертер отдаёт "северная,восточная", а у чертежа этого
     проекта X = зона МСК-47 (восточная, ~2 250 000), Y = северная
     (~450–460 тыс.) — проверено разбором реального текстового объекта
     на подоснове (см. README); `LEP-GEOPOINTS-DRAW` эту перестановку
     делает явно.
  4. `LEP-GEOPOINTS-CLEAR` — стирает все объекты со слоя одной командой
     (слой остаётся, не пересоздаётся).
  **✅ Разбор GeoJSON подтверждён живым тестом** (2026-07-20, первый
  реальный запуск `LEP-GEOPOINTS-EXTRACT` пользователем) — предыдущая
  попытка теста срывалась из-за глюка автозавершения командной строки
  AutoCAD при прямой вставке кода (см. README), не из-за логики; сам
  парсер отработал корректно.

## Временные решения (см. README для деталей)

- **⚠️ Угол разворота откоса** у 2-/3-столбовых опор (`LEP-POLESYM.LSP`,
  `LEP:InsertPoleGroup`) — черновая прикидка (перпендикулярно маршруту,
  поворот 90° от главной опоры), точный угол уточняется у коллеги
  пользователя. Когда придёт точный угол — поправить ТОЛЬКО эту
  функцию.
- **⚠️ Блок символа опоры не передефайнится** при повторной загрузке
  (в отличие от слоёв) — см. README, раздел с восклицательными
  знаками, если нужно обновить геометрию после правок.
- Объект-пролёт из `LEP-SPAN` НЕ связан через `GEOMCONSTRAINT` с
  опорами — при переносе опоры пролёт придётся перечертить заново.

## Известные открытые проблемы

- **✅ КАБЕЛЬ БЕЗ ОПОР + КАБЕЛЬНЫЙ КИОСК (2026-07-24)** — раньше
  кабельный пролёт строился ТОЛЬКО между двумя опорами. Пользователь
  принёс схему «КТП → два кабеля 0,4 кВ → киоск с двумя ПУ», где опор
  нет вообще. Оказалось дёшево: КТП/киоск ставятся ТЕМ ЖЕ способом,
  что опора (`LEP:InsertVertexOnRoute` + `GEOMCONSTRAINT`), то есть
  это такой же блок на вершине маршрута с хендлом и `ROUTE`, а вся
  механика пролётов работает по хендлам/вершинам и о типе не
  спрашивает. Правка — одна строка: `LEP:PickPole` принимает `TYPE` ∈
  `POLE`/`KTP`/`KK`. ⚠️ Попутно найден и исправлен **настоящий баг**:
  два пролёта между ОДНОЙ парой концов (два кабеля, или два провода на
  одной паре опор — механику параллельного сдвига от 2026-07-22
  вживую не гоняли) **сливались в одну подпись с суммой длин** —
  `LEP:GetSpanNeighbors` считает соседом любого, кто делит хотя бы
  один хендл, а у близнеца совпадают ОБА. Теперь такие исключаются из
  объединения, у каждого своя подпись. Также `LEP:AddKLPoleNote`
  проверяет `TYPE = "POLE"` — фраза «+ подъём кабеля по опоре» на
  наземной КТП неверна (сегодня она и так не появлялась, но лишь
  потому, что у КТП нет `LABELHANDLE` — везение, а не замысел).
  ⚠️ Заодно починена высокая сторона КТП: раньше бралась из класса
  маршрута и на 0,4-кВ трассе давала мусор «160/04/0,4 кВ» — КТП это
  граница двух классов, теперь спрашивается отдельно
  (`*LEP-KTP-HighSide*`, ключ `HIGHSIDE`). См. README.
- **⚠️ НЕ ПРОВЕРЕНО ВЖИВУЮ (2026-07-24)**: кабельный киоск, пролёт
  между наземными объектами, поконцовый запас, фикс параллельных
  близнецов и высокая сторона КТП — ни одного прогона не было.
- **⚠️ НЕ ПРОВЕРЕНО ВЖИВУЮ (2026-07-24)**: новые нормы запаса
  (`LEP:GetRealLength`/`LEP:GetPhaseCount`), разделение "чертёж vs
  ведомость" (`LEP:GetDisplayLength`) и подсчёт труб ГНБ (ЭТАП 1.7)
  написаны, но ни одного прогона в AutoCAD не было. Что проверить:
  подписи на чертеже НЕ изменились (ВЛ сырая, КЛ сырая+6, без ×3);
  ВЛ 10 кВ в ведомости ≈ втрое больше, чем в подписи; появилась строка
  "Труба для ГНБ-прокола (13 м)"; прокол из двух пролётов подряд даёт
  ОДНУ запасную трубу.
- Марки СИП-3 в `*LEP-WireTypes-VL-10*` — введены "на правдоподобный
  вид" по референсу пользователя, не сверены с реальным каталогом.
- **✅ РЕШЕНО 2026-07-16**: мощность МТП раньше была зашита жёстко
  ("МТП-160 кВА") — оказалось, у заявителей встречается и "МТП-250/
  10/0,4" (формат "мощность/ВН/НН"). Теперь `*LEP-MTP-Types*`
  (`LEP-CONFIG.LSP`) — список выбора, спрашивается один раз при
  постановке МТП в `LEP-END`. ⚠️ Важно — не просто добавили строку в
  список: раньше ведомость вычисляла название МТП ЗАНОВО из конфига
  при каждой генерации отчёта (XDATA хранила только общий код "MTP"),
  при нескольких мощностях все МТП слились бы в одну строку. Теперь
  для НОВЫХ МТП выбранная строка целиком пишется в XDATA как `EQUIP` —
  разные мощности считаются отдельными строками. Старые МТП (код
  просто `"MTP"`) — обратная совместимость через `*LEP-Label-MTP*`
  (переформатирован в тот же стиль "мощность/ВН/НН", было "МТП-160
  кВА" без классов напряжения), см. `LEP:GetEquipLabel` в
  `LEP-REPORT.LSP`. **✅ ДОПОЛНЕНО 2026-07-24** — в тот же список
  добавлены МТП на 6 кВ; отдельными строками ведомости они становятся
  автоматически, ровно благодаря механизму "строка целиком в EQUIP",
  описанному выше.
- **✅ РЕШЕНО 2026-07-22: прерывание во время рисования PLINE в
  `C:LEP-ROUTE`** срывало создание маршрута молча (подпись не
  появлялась, опора получала фиктивный `LEP_ROUTE_UNKNOWN`) - найдено
  пользователем 2026-07-15 (Shift+ПКМ), повторно поймано 2026-07-22 с
  чистым логом (обычный Escape/`*Cancel*` во время `PLINE`). Механизм
  раскрыт построчным чтением кода: цикл ожидания `(command pause)`
  прерывался условием "Function cancelled", которое ловил СВОЙ `*error*`
  команды (заведён 2026-07-11 для другой цели) - а `*error*` в AutoLISP
  после своей работы завершает ВСЮ функцию целиком, поэтому проверка
  `RouteEnt`/громкое предупреждение (фикс 2026-07-15) просто не успевали
  сработать. Исправлено - цикл ожидания обёрнут в `vl-catch-all-apply`
  (ловит прерывание БЕЗ вызова `*error*`), выполнение доходит до
  существующей проверки. См. README для полной истории.
- После одного прогона `LEP-SCALE` у пользователя оказались выключены
  два слоя — код-ревью не нашло в проекте механизма, способного это
  сделать (кроме `LEP-ROUTEVIS`/`LEP:FindNearestRoute`, которые трогают
  только слой маршрута). Не воспроизвелось повторно, причина не
  найдена — отложено, см. README для деталей на случай повтора.
- **Отступ перед текстом подписи (`LEADER`) — ✅ РЕШЕНО 2026-07-11**:
  причиной была полка LEADER длиной = размеру стрелки (`DIMASZ`,
  дефолт ISO-25 = 2.5 → 2,5 МЕТРА в масштабе 1 ед. = 1 м). Лечится
  `(setvar "DIMASZ" *LEP-Dim-ArrowSize*)` в `LEP-SETUP` (0,8 в
  CONFIG). `DIMGAP`/шрифт были ни при чём; гипотеза про
  TrueType-рамку опровергнута живым тестом. ⚠️ `isocp.shx` НЕ содержит
  кириллицу — не предлагать SHX-шрифты для русских подписей. Полная
  история — в README. **С 2026-07-15 подписи опор/маршрута/пролёта
  создаются через `MLEADER`, не `LEADER`** (см. отдельную запись ниже) —
  `DIMASZ` больше НЕ отвечает за отступ у подписей, но всё ещё нужен
  для стрелок `DIMALIGNED` (размерная линия пролёта, `LEP-SPAN.LSP`) —
  трогать/убирать нельзя.
- **Миграция подписей `LEADER` → `MLEADER` — ✅ РЕАЛИЗОВАНО 2026-07-15**:
  стрелка+полка+текст теперь один объект, текст лежит на полке/
  подчёркнут (настройка стиля "Standard", `LEP:CreateMLeaderStyle` в
  `LEP-SETUP.LSP`). Найденные живым тестом группы DXF стиля
  `MLEADERSTYLE`: 43=полка, 44=стрелка, 45=высота текста, 42=зазор,
  93=цвет (ByLayer), 174/178/295/298=подчёркивание (⚠️ последние 4 —
  подтверждены только КОМБИНАЦИЕЙ, не по отдельности), 342=текстовый
  стиль (наклон 10°/шрифт ISOCPEUR - ✅ подтверждено 2026-07-15,
  вычисляется через `tblobjname "STYLE"` заново при каждой загрузке,
  СТРОГО после `LEP:CreateTextStyle`). Полная история тестов и
  таблица значений — в README, раздел "✅ Миграция подписей
  LEADER → MLEADER". Открыто: размер стрелки 0.8 не сверен визуально
  на реальном чертеже (в отличие от DIMASZ 0.8, который сверяли).
- **✅ Background mask (фон под текстом выносок) — РЕАЛИЗОВАНО
  2026-07-15**: НЕ свойство `MLEADERSTYLE` (сверено с полным списком
  атрибутов у `ezdxf`), а свойство блока `CONTEXT_DATA` каждой
  ОТДЕЛЬНОЙ сущности `MULTILEADER` — группы 91/141/291/292, найдены
  живым entget-диффом. `LEP:SetMLeaderBackgroundMask` (`LEP-UTILS.LSP`)
  вызывается после каждого `_.MLEADER` через `(entlast)`. ⚠️ ВАЖНАЯ
  ЛОВУШКА: у `MULTILEADER` DXF-коды повторяются на разных уровнях
  вложенности (`CONTEXT_DATA{`/`LEADER{`/`LEADER_LINE{`) — обычный
  `assoc`/`subst` по всему списку задевает чужие одинаковые по значению
  поля (поймано живым тестом на группе 292). Функция правит только
  "голову" списка до маркера `(302 . "LEADER{")`.
- **✅ Draw Order Front (2026-07-15)**: `LEP:BringToFront`
  (`LEP-UTILS.LSP`) — `DRAWORDER`/`_Front` через `command` (`Ent`
  напрямую как выбор объекта, как и `GEOMCONSTRAINT`). Вызывается
  после каждой подписи (опоры/маршрута/пролёта) и размерной линии
  пролёта — не рискуют оказаться визуально под линией/штриховкой.
- **Заливка символа опоры (`SOLID`)** — подтверждено рабочим тестом в
  консоли (порядок вершин 1-2-4-3, не по периметру), но НЕ
  интегрирована в `LEP-POLESYM.LSP` — отложено по решению пользователя,
  подход рабочий, можно добавить в любой момент.
- **✅ `LEP-GEOPOINTS.LSP` (2026-07-20) — парсер GeoJSON подтверждён
  живым тестом** (первый реальный запуск `LEP-GEOPOINTS-EXTRACT`
  пользователем прошёл успешно). Открыто: перестановка X/Y и сама
  привязка кружков к реальной подоснове визуально ещё не сверены (см.
  README, раздел "GPS-точки из GeoJSON" — рекомендация сверить хотя бы
  одну характерную точку после `LEP-GEOPOINTS-DRAW`).
- **✅ Толщина линии/чинилка подписей/без клика по маршруту (2026-07-22)**
  — `LEP:SetEntityLWeight` теперь применяется и к пролётам, и к КТП (не
  только к опорам). Новая команда `LEP-SPAN-REPAIR` чинит подписи
  пролётов, "осиротевшие" после ручного `ERASE` (сканирует все трассы,
  пересчитывает через `LEP:FinalizeSpanLabel` — не чинит подписи,
  которые физически не стёрты, а просто стали неверными — осознанно).
  `C:LEP-SPAN` больше не спрашивает маршрут кликом — определяет его по
  `ROUTE` первой выбранной опоры (`LEP:FindRouteByID`), с проверкой,
  что вторая опора той же трассы. **✅ Т-образное подключение веток**
  (2026-07-22, дырка замечена пользователем) — если трассы у двух опор
  разные, команда не отказывает намертво, а переспрашивает "это
  намеренная ветка?"; если да — пролёт считается прямой линией P1-P2
  (`Verts = (list P1Pt P2Pt)` вместо вершин реальной трассы), привязка
  к трассе для ведомости — по первой кликнутой опоре. См. README для
  деталей.
- **✅ Геометрическое определение маршрута пролёта (2026-07-22)** —
  если маршрут 2 проложен ПОВЕРХ опор маршрута 1 (общий участок двух
  фидеров), опора в XDATA помнит только маршрут 1 (тот, которым её
  создали), и `LEP-SPAN` молча относил новые пролёты туда же.
  `LEP:FindRoutesThroughPoints` теперь ищет, какие трассы ФИЗИЧЕСКИ
  проходят через обе кликнутые опоры (допуск
  `*LEP-Route-Detect-Tolerance*`, 1 м — завышен сознательно, т.к.
  вторая полилиния рисуется руками): 1 кандидат — молча, несколько —
  один вопрос с запоминанием ответа на сессию, ноль — откат на `ROUTE`
  опоры. Геометрия/класс/`RouteID` берутся у ВЫБРАННОГО маршрута,
  поэтому ведомость и объединение подписей работают дальше без правок.
  ⚠️ Опоры общего участка остаются за маршрутом 1 (осознанно —
  маршруту 2 там нужен только провод). См. README.
- **✅ Параллельный сдвиг проводов на одной паре опор (2026-07-22)** —
  если маршрут 2 идёт через те же опоры, что маршрут 1 (общий участок,
  дальше расходятся), или просто два провода на одном маршруте между
  одной парой опор — без сдвига оба пролёта легли бы геометрически
  точно друг на друга (нечитаемо), хотя данные честно разделены.
  `LEP:FindSamePolePairSpans` находит уже существующие пролёты на той
  же паре опор (по `POLE1HANDLE`/`POLE2HANDLE`), новый сдвигается
  перпендикулярно на 0,5м x номер по счёту, поочерёдно в разные
  стороны (`*LEP-Span-ParallelOffset*`, `LEP-CONFIG.LSP`). Размерная
  линия рисуется только у первого пролёта на паре опор. См. README.
- **✅ КТП — наземная подстанция (2026-07-22, доработано по правке
  пользователя на месте)** — новый пункт `LEP-END`, НЕ опора:
  прямоугольник 3х5м (пока прикидка) + треугольник-значок
  трансформатора, растянутый на всю высоту (широкая часть = верхний
  край целиком, остриё = нижний край целиком), заливка одним цветом
  без градиента. Прямоугольник+треугольник — ОДИН блок-символ
  (`LEP-KTPSYM.LSP`, по образцу `LEP-POLESYM.LSP`), не два отдельных
  объекта, как в первой версии — заодно снимает вопрос о надёжности
  `GEOMCONSTRAINT` (теперь тот же тип сущности — INSERT блока — что и
  у опоры). Слои — ранее простаивавшие `*LEP-Layer-Proj-TP*`/
  `*LEP-Layer-Exist-TP*`. Номер/мощность/марка — свободный ввод.
  Ведомость — ЭТАП 2.6 в `LEP-REPORT.LSP` (сканирует `INSERT`, не
  `LWPOLYLINE`). См. README для деталей.
- **✅ СТП + подпись МТП между опорами (2026-07-22)** — СТП (трансформатор
  на одной опоре) новым пунктом в `LEP-END`, пока текстом ("+ СТП-XX
  кВА" второй строкой, без квадратика-графики — отложено). Подпись МТП
  теперь указывает на середину между опорами 2 и 3 (там физически
  висит трансформатор), а не на саму опору 2 — сами опоры не
  переехали, см. README.
- **✅ Третий класс напряжения "6 кВ" (2026-07-22)** — добавлен в
  `*LEP-VoltageClasses*`/`*LEP-VoltageClassLabel*`, новый список марок
  `*LEP-WireTypes-VL-6*` (⚠️ марки скопированы с 10кВ, не подтверждены
  пользователем), выбор марки в `LEP-SPAN.LSP` переделан с бинарного
  `if` на `cond` из трёх веток (иначе "6" молча попало бы в ветку
  0,4кВ). Цвет слоя для 6кВ сознательно НЕ свой — пока переиспользует
  10кВ (решение пользователя, "дальше видно будет"), см. README.
- **✅ True Color (RGB) для новых слоёв ВЛ/опор/КЛ (2026-07-22) —
  ПОДТВЕРЖДЁН живым тестом.** По итогам просмотра программы
  пользователем добавлены цвета по классу напряжения x статус — три из
  пяти новых заданы как RGB (не ACI). Пользователь прогнал тестовый
  слой (`entmod` DXF-группы 420) — диспетчер слоёв AutoCAD показал
  реальный фиолетовый цвет, не серый/дефолтный. `LEP:SetLayerTrueColor`
  (`LEP-SETUP.LSP`) теперь применяется ко всем 4 затронутым слоям
  (0,4кВ Сущ. ВЛ/опоры, КЛ ГНБ, КЛ Траншея) сразу после
  `LEP:CreateLayer` — см. подробный разбор в README, раздел "Правки по
  итогам просмотра готовой программы".

## Правила работы с кодом в этом проекте

- **⚠️ ЕСЛИ НЕ УВЕРЕН В ТОЧНОМ ПОВЕДЕНИИ/СИНТАКСИСЕ — СНАЧАЛА ПРОВЕРЬ
  МАЛЕНЬКИЙ КУСОЧЕК ЖИВЬЁМ В КОНСОЛИ AUTOCAD, ПОТОМ ПИШИ В ФАЙЛ.**
  Это касается: точного порядка аргументов `command` (уже ловили баги
  на `LEADER`, `-linetype`), единиц измерения углов (радианы/градусы —
  не одинаково для всех DXF-групп, например у `STYLE` угол наклона в
  градусах, а у большинства остальных entmake-углов в проекте —
  радианы), точной структуры `entmake` для таблиц (`STYLE`/`LAYER` и
  т.п. требуют маркеров подкласса, которых легко не досчитаться),
  доступности шрифтов/ресурсов в конкретной установке AutoCAD. Если
  сам не можешь протестировать (нет живой консоли под рукой) — дай
  пользователю МАЛЕНЬКИЙ, изолированный кусок кода/команду, попроси
  вставить в командную строку AutoCAD и показать результат, и только
  ПОСЛЕ этого пиши финальную версию в файл. Не полагайся на память о
  DXF-документации там, где реальный тест возможен и дёшев — в этом
  проекте это правило уже несколько раз забывалось "в комментариях",
  но не было явно записано здесь, из-за чего нарушалось снова.
- Это AutoLISP (Visual LISP диалект AutoCAD), а не обычный Lisp —
  используются функции `vl-*`, `vla-*` (ActiveX, только Windows) и
  DCL-диалоги. При правках проверяй кроссплатформенность (Mac не
  поддерживает ActiveX).
- Изменения тестируются вручную в AutoCAD через `APPLOAD` —
  автоматических тестов нет. После значимых правок обнови раздел
  "Что уже готово и проверено" / "Известные открытые проблемы" в
  [LEP-README.md](LEP-README.md).
- Не хардкодь пути пользователя — вся загрузка модулей идёт через
  Support Path / `findfile`, см. `LEP-START.LSP`.
- В git идут только исходники (`.LSP`/`.DCL`/`.LIN`/`.md`) — DWG,
  lock-файлы (`.dwl`/`.dwl2`), бэкапы и выгруженные ведомости в
  `.gitignore`, не коммить их (см. `.gitignore` в корне).
- Перед добавлением новой функции проверь, нет ли уже похожей в
  `LEP-UTILS.LSP`/`LEP-POLYLINE.LSP` — в проекте уже случалось
  дублирование (`LEP:GetVertexList`/`LEP:GetVertices` делали одно и
  то же в разных файлах), и мёртвый код, который никто не вызывал.
- Комментарии в коде и описания в README — максимально подробные.
  Пользователь не программист-профессионал и сам почти не пишет
  AutoLISP, поэтому важно объяснять не только ЧТО делает код, но и
  ПОЧЕМУ именно так (особенно если решение неочевидное или это фикс
  конкретного бага) — это относится и к новым функциям, и к правкам
  существующих.

## Режим подтверждения правок

Перед любой работой сначала пиши план: что нашёл/понял и что
собираешься менять и почему. Дальше — по ситуации:

- Если правка однозначно вытекает из того, что показал сам
  пользователь (лог ошибки, скриншот, описание бага) и реального
  выбора нет — делай сразу, без паузы на подтверждение, и отчитайся,
  что сделал.
- Если есть настоящая развилка (несколько разумных вариантов
  реализации, дизайн-решение, что-то не тривиально отменить/легко
  сделать зря) — остановись и спроси, какой вариант нужен, ПЕРЕД тем
  как редактировать файлы.
